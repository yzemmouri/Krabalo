const gameWidth = 1600.0;
const gameHeight = 820.0;
const ballRadius = gameWidth * 0.02;
const sensitivity = 4000;
const gravity = 800.0;
const maxBounces = 6;
const restitution = 0.7; //Coefficient of restitution (elasticity)
const energyLoss = 0.85; // Energy retention factor per bounce
